# Процесс работы с ИИ и проверками в проекте fio-cleaner

Этот документ фиксирует договорённости о том, как мы работаем с ИИ,
терминалом и проверками результатов, чтобы процесс был понятным,
воспроизводимым и не перегружал внимание.

---

## Основные правила

### 1. Один шаг — одна команда
Каждый шаг формулируется как одно конкретное действие в терминале.
Следующий шаг определяется только после анализа результата предыдущего.

---

### 2. Проверки выполняются явно
Если требуется проверить результат (например: `git diff`, `git status`,
`git log`, вывод команды), проверка выполняется отдельной командой.

Мы не смешиваем:
- действие;
- и его проверку.

---

### 3. Визуальное выделение проверок

Если результат команды нужно увидеть и обсудить,
перед проверочной командой используется визуальный маркер.

Формат:

(несколько пустых строк)

🟢 CHECK: ОПИСАНИЕ ПРОВЕРКИ КАПСОМ

После маркера идёт команда проверки.

Это правило позволяет быстро находить нужный вывод
в потоке терминальных логов.

---

### 4. Когда правило обязательно

Правило визуального маркера используется для:
- `git diff`
- `git status` / `git log`
- любых проверок, результат которых анализируется совместно с ИИ

---

## Цель этих правил

- снизить когнитивную нагрузку;
- сделать выводы легко читаемыми;
- обеспечить воспроизводимость шагов;
- упростить продолжение работы в новых диалогах и на следующих этапах проекта.
